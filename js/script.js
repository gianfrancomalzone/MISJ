/*

const Scrolls = document.querySelectorAll(".scroll");

const checkScrolls = () => {
    const triggerBottom = (window.innerHeight / 5) * 4;
    Scrolls.forEach((scroll) => {
        const scrollTop = scroll.getBoundingClientReact ().top;
        if (scrollTop < triggerBottom) scroll.classList.add("show");
        else scroll.classList.remove("show");
    });
};

window.addEventListener("scroll", checkScrolls);
checkScrolls()

*/

